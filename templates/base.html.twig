<!DOCTYPE html>
<html>
    <head>
        <meta charset="UTF-8">
        <meta name="viewport" content="width=device-width, initial-scale=1.0">
        <title>{% block title %}Carnet d'Adresses{% endblock %}</title>
        <link rel="stylesheet" href="{{asset('assets/css/style.css')}}">
        {# Initalisation des fichier bootstrap #}
        <link rel="stylesheet" href="{{asset('assets/css/bootstrap.min.css')}}">
        <link rel="stylesheet" href="{{asset('assets/css/bootstrap.min.css.map')}}">
        {% block stylesheets %}
        {% endblock %}

        {# Initalisation des fichier bootstrap javascripts #}
        <script src="{{asset('assets/javascript/bootstrap.bundle.min.js')}}"></script>
        <script src="{{asset('assets/javascript/bootstrap.bundle.min.js.map')}}"></script>
        {% block javascripts %}
        {% endblock %}
        

    </head>
    <body>
    {% if app.request.attributes.get('_route') not in ['app_register', 'login'] %}
        {% include 'commons/_nav.html.twig' %}
        {# Inclure la barre de navigation ici #}
    {% endif %}
        {% block body %}

        {% endblock %}
    </body>
</html>